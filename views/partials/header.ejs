
<style>
  .input-group-text {
  border-radius: 999px 0 0 999px !important; 
  border: none !important;
  background-color: white !important;
  padding-left: 1rem;
  padding-right: 0.75rem;
}

.form-control {
  border-radius: 0 999px 999px 0 !important; 
  border: none !important;
  background-color: white !important;
  padding-left: 0;
}

.input-group {
  overflow: hidden;
  border-radius: 999px;
  background-color: white;
  box-shadow: 0 0 5px rgba(0, 0, 0, 0.05); 
}

</style>
<% /* —————— Flash Messages —————— */ %>
<% if ((success && success.length) || (error && error.length)) { %>
  <div class="container mb-2">
    <% if (success && success.length) { %>
      <div class="alert alert-success alert-dismissible fade show">
        <%= success[0] %>
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
      </div>
    <% } %>
    <% if (error && error.length) { %>
      <div class="alert alert-danger alert-dismissible fade show">
        <%= error[0] %>
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
      </div>
    <% } %>
  </div>
<% } %>

<header class="py-3 ">
  <div class="container d-flex align-items-center">
    <% if (typeof expGroup !== 'undefined') { %>
      <div class="me-3">
        <span class="px-3 py-2 rounded-pill shadow-sm" 
              style="font-size: 0.85rem; font-weight: bold; font-family: 'Boska', serif;
                     <%= expGroup === 'A' 
                         ? 'background-color: #f7f1ec; color: #00766d; border: 2px solid #d8c2b3;' 
                         : 'background-color: #fffade; color: #743f4d; border: 2px solid #d8c2b3;' %>">
          <%= expGroup === 'A' ? '✏️ Edit Mode' : '👁️ View Mode' %>
        </span>
      </div>
    <% } %>
   

    <form action="/search" method="GET"
          class="me-4 flex-grow-1"
          style="max-width:7000px;">
      <div class="input-group shadow-sm">
        <span class="input-group-text bg-white border-end-0">
          <i class="bi bi-search"></i>
        </span>
        <input
          type="search"
          name="q"
          class="form-control border-start-0"
          placeholder="Search users, posts or tags…"
          value="<%= query||'' %>"
          
          
        />
      </div>
    </form>


  </div>
</header>
